const array = [1, "strings", true]; //constitucion array

const posicion = 1; //asignacion valor

console.log(array[posicion]); //acceso a valor

array[posicion] = "hola"; // reasigno el valor de la posicion

console.log(array[posicion]);

const numbers = [1, 2, 3, 4, 5];
numbers[0] = 10;
numbers[2] = 30;
console.log(numbers);
